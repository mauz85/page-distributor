[Unit]
Description=PageDistributor
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker run --rm -p 80:80 -p 443:443 --name page-distributor page-distributor /usr/sbin/apache2ctl -D FOREGROUND

[Install]
WantedBy=multi-user.target
